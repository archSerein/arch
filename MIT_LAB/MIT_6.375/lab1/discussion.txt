Question:
1.
bluespec源码中使用for循环来生成FIR滤波器硬件会影响生成的硬件结构以及其性能指标，如面积和关键路径延迟.
	不使用for循环: 每个抽头通常是单独实例化的。这导致更直接但可能重复的硬件结构，每个乘法器和加法器都是手动实例化的。
	使用for循环: 硬件描述变得更加模块化和紧凑。for循环抽象了重复操作，允许编译器以编程方式生成重复结构。
	不使用for循环: 由于每个元素都是手动实例化的，可以有更多的优化机会，通过手动调整。然而，如果没有优化，也可能导致冗余逻辑。
	使用for循环: 生成的硬件由于抽象可能初看起来更紧凑，但实际物理面积可能会增加，因为缺少手动优化的机会。相反，for循环也可以使编译器更有效地进行全局优化，可能减少冗余逻辑。
	不使用for循环: 由于每个组件是单独实例化的，关键路径延迟可能更容易管理，允许对时间路径进行精确控制。
	使用for循环: 由于引入了统一的抽象，关键路径延迟可能增加，使得特定时间路径优化变得更加困难。然而，如果编译器进行了积极的优化，可能会平衡或甚至减少关键路径延迟。
2. 

