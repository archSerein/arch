module part2(
    input a,
    input b,
    output [2:0] O
);

    wire nota, notb;
    not na(nota, a);
    not nb(notb, b);

    and and_1(O[0], a, notb);
    and and_2(O[2], notb, a);
    xnor xnor_3(O[1], a, b);

endmodule
